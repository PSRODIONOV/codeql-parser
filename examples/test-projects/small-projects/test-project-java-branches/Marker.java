// Технический файл (default package, вне branches/): без него все файлы
// проекта лежат в ОДНОМ каталоге, и detect_db_prefix() (core/file_lists.py)
// при извлечении исходников из src.zip срезает общий префикс ВКЛЮЧАЯ имя
// пакета "branches" целиком (т.к. он становится "общим хвостом" пути у
// всех трёх .java) — извлечённое дерево разваливалось бы в одну плоскую
// директорию без branches/, и javac/CodeQL package-структуру не нашли бы.
// Присутствие файла-соседа ВНЕ branches/ останавливает срез на уровне
// корня проекта, сохраняя branches/ при извлечении.
final class Marker {
}
