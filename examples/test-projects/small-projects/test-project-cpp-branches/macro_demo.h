#ifndef MACRO_DEMO_H
#define MACRO_DEMO_H

// Демонстрация двух макро-случаев для проверки правил инструментатора:
//   1) ФО ЦЕЛИКОМ генерируется макросом — должен ИСКЛЮЧАТЬСЯ из перечня ФО и
//      не инструментироваться (нет отдельного места в исходнике);
//   2) тело ФО (открывающая { и пролог) генерируется макросом, имя настоящее —
//      должен инструментироваться (датчик входа с учётом макро-{).
// call_macro_demo вызывает все, чтобы они попали в сборку/БД.
int call_macro_demo(void);

#endif // MACRO_DEMO_H
